{
	"version": 2,
	"builds": [
		{
			"src": "dist/server.js",
			"use": "@vercel/node",
			"config": { "includeFiles": ["dist/**"] }
		}
	],
	"routes": [
		{ "handle": "filesystem" },
		{ "src": "public/(.*)", "dest": "public/$1" },
		{ "src": "/(.*)", "dest": "dist/server.js" }
	]
}

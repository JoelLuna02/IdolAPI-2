{
	"version": 2,
	"builds": [
		{
			"src": "build/server.js",
			"use": "@vercel/node",
			"config": { "includeFiles": ["build/**"] }
		}
	],
	"routes": [
		{ "handle": "filesystem" },
		{ "src": "public/(.*)", "dest": "public/$1" },
		{ "src": "/(.*)", "dest": "build/server.js" }
	]
}
